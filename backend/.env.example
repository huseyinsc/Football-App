# Database Credentials (Do not share real passwords here!)
PGUSER=your_username_here
PGPASSWORD=your_password_here

# Security
# Base64-encoded JWT signing key, preferably at least 32 bytes before encoding.
JWT_SECRET=replace_with_base64_encoded_secret

# Optional: token lifetime in milliseconds
JWT_EXPIRATION=86400000
